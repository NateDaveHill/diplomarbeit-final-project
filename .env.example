# Database Configuration
# Use 'sqlite' (no installation needed) or 'mysql'

# Local Development (using devenv)
DB_TYPE=mysql
DB_HOST=127.0.0.1
DB_NAME=webshop_edv
DB_USER=webshop
DB_PASS=webshop

# SQLite Configuration (backup - not currently used)
#DB_TYPE=sqlite
#DB_PATH=/path/to/your/database.sqlite

# Session Configuration
SESSION_NAME=webshop_session

# Application Configuration
APP_ENV=development
APP_DEBUG=true

# Railway Deployment Note:
# On Railway, you don't need to set these variables manually.
# Railway will automatically provide a DATABASE_URL environment variable
# when you add a MySQL database service and link it to your web service.
# The application will automatically parse and use DATABASE_URL on Railway.